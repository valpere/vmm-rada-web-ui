// Test setup file run by Vitest before each test file (setupFiles in vite.config.js).
// Adds jest-dom's custom matchers (toBeInTheDocument, toBeDisabled, …) to expect.
import '@testing-library/jest-dom';
